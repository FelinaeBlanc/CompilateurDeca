{
    int secret, user;
    secret = 421;
    println("Quel est mon nombre entre 0 et 1000 ?");
    user = readInt();
    
    while (! (user == secret)) {
        if (user < secret) {
            print("Trop petit !");
        } else if (user > 8) {
            print("Trop grand !");
        }

        if (user == 2 && secret < user){

        }else if (user == 2) {

        }

        println(" Autre proposition ?");
        user = readInt();
    }
    println("Bravo...");
}