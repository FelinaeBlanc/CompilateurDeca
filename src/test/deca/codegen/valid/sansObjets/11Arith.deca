{
    float x, m, fm;
    int y;
    float g = 0.0f, d = 1.0f;
    

    x = 10.0 / 2.5 + 3.0 - 40.0;
    println(x);

    y = 34 * 1 * (12/4) ;
    println(y);

    y = y * (y * (y * (10/5) - (10/5)) - (10/5) ) - (10/5);
    println(y);

    x = (x + x) + 2.0f;
    
    println(x);

    m = (g+d) / 2.0f;

    println(m);

    fm = m * (m * (m * (m * (m * (m * ( m * (1.0/5040.0) + 1.0/720.0) + 1.0/120.0) + 1.0/24.0) + 1.0/6.0) + 1.0/2.0) + 1.0) -1;

    println(fm);
}