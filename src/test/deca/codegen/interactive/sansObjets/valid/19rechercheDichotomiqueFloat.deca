{
    float secret, user;
    secret = 421.2;
    println("Quel est mon nombre entre 0 et 1000 ?");
    user = readFloat();
    while (! (user == secret)) {
        if (user < secret) {
            print("Trop petit !");
        } else {
            print("Trop grand !");
        }
        println(" Autre proposition ?");
        user = readFloat();
    }
    println("Bravo...");
}